// Vercel Build Output API function entry — static SPA.
// Since BrandForge AI is a SPA (no SSR), this entry is not bundled into a serverless
// function during deploy. The static output is served directly with SPA rewrites.
// This file is kept as a minimal placeholder; the build-vercel.sh script handles the
// actual deployment assembly.
export default {};
